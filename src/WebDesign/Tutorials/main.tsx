import React from 'react'
import ReactDOM from 'react-dom/client'

import '../../index.css'
import WebDesignTutorial from './tutorials'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WebDesignTutorial />
  </React.StrictMode>,
)
